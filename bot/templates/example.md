---
title: Сортировка массивов
subject: Алгоритмы и структуры данных
work_number: 3
---

# Введение

Целью данной лабораторной работы является изучение основных алгоритмов сортировки массивов и сравнение их эффективности на различных наборах данных.

В ходе работы были реализованы следующие алгоритмы:

- Сортировка пузырьком (Bubble Sort)
- Сортировка вставками (Insertion Sort)
- Быстрая сортировка (Quick Sort)

# Теоретическая часть

## Сортировка пузырьком

Сортировка пузырьком — простейший алгоритм сортировки. Он проходит по массиву несколько раз, каждый раз перемещая наибольший элемент в конец.

**Сложность:** O(n²) в среднем и худшем случае.

## Быстрая сортировка

Быстрая сортировка использует подход «разделяй и властвуй». Выбирается опорный элемент, после чего массив разбивается на две части.

**Сложность:** O(n log n) в среднем случае, O(n²) в худшем.

# Практическая часть

## Реализация

Реализация алгоритма быстрой сортировки на Python:

```python
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)
```

## Результаты тестирования

Сравнение времени работы алгоритмов (в миллисекундах):

| Алгоритм | n=100 | n=1000 | n=10000 |
|-----------|-------|--------|---------|
| Пузырёк  | 0.5   | 45.2   | 4520.1  |
| Вставки   | 0.3   | 22.1   | 2210.5  |
| Быстрая   | 0.1   | 1.2    | 15.3    |

# Выводы

1. Быстрая сортировка показала наилучшие результаты на всех размерах входных данных
2. Сортировка пузырьком является самой медленной из рассмотренных алгоритмов
3. Сортировка вставками показывает приемлемые результаты на малых массивах

# Список использованных источников

1. Кормен Т. и др. *Алгоритмы: построение и анализ*. — М.: Вильямс, 2013.
2. Седжвик Р. *Алгоритмы на Java*. — М.: Вильямс, 2016.
